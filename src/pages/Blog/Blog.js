import React from 'react';
import Styles from '../../components/About/aboutus.module.scss';

const Blog = () => {
  return (
    <div>
      <div className={Styles.titleWrapper}>
        <p className={Styles.title}>Blog</p>
      </div>
    </div>
  );
};

export default Blog;
